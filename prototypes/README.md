# Prototypes

This directory contains local-first prototype entrypoints built on top of the substrate.

Current prototype:

- `continuity-workstation.ts`

Run:

```bash
npm run prototype:workstation
```

The prototype is intentionally local and small. Its job is to exercise multiple substrate concepts together under realistic pressure without requiring decentralized backends.
