# Adjacent Layer Public Lifecycle Smoke Observation

Proof rung of this Causal operation: `saved_readback_seam`.

Source material:
`../mesh-ecology-layer/proof-artifacts/layer-public-seam-lifecycle-smoke-latest.json`

The source Layer material reports a public/default HyperDHT lifecycle smoke with
`up/status/down`, durable reopened readback, linked request/receipt/evidence
counts, and strongest source rung
`default_public_hyperdht_hyperswarm_feed_backed`.

Causal preserved the refs present in that material, including latest request
hash, latest receipt hash, latest evidence ref, durable storage/topic/autobase
refs, writer ref, proof rung, and linkage counts. The Layer smoke did not
expose request or receipt ids, so Causal did not invent them.

This observation does not open swarm, open Corestore, call Layer, call Edge,
admit Layer evidence, interpret RBC, grant authority, publish Mesh, write
production continuity, or claim live Causal public-swarm proof.

Next pressure: run `npm run public-observer -- observe` against a fresh
Edge/Layer lifecycle source manifest or equivalent Causal-observable public
descriptor when one exists.
