# Public Observer Descriptor Preflight

Proof rung: `local_artifact_seam`.

This directory records one enabled `public-observer observe` preflight over the
current retained Edge and Layer descriptor material:

- Edge descriptor:
  `../mesh-ecology-edge/proof-artifacts/edge-causal-spine-handoff-readiness-2026-06-03/second-host-compatible-endpoint-descriptor.json`
- Layer descriptor:
  `../mesh-ecology-layer/proof-artifacts/layer-public-proof-export-bundle-latest/artifacts/public-endpoint.json`

Result: `blocked_waiting_for_causal_observable_source_manifest`.

The command did not open swarm, open Corestore, write Edge state, write Layer
state, admit Layer evidence, interpret RBC, grant authority, publish Mesh, or
write production continuity. It exists to prevent treating retained descriptor
or endpoint material as live Causal public-swarm observation.

Next pressure: run `npm run public-observer -- observe` against a fresh
Edge/Layer lifecycle source manifest or descriptor that Causal can actually
read through the declared public Hyperswarm path.
